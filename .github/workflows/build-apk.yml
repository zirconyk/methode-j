name: Build APK

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'
        cache: 'npm'

    - name: Setup Java JDK
      uses: actions/setup-java@v3
      with:
        distribution: 'temurin'
        java-version: '11'

    - name: Setup Android SDK
      uses: android-actions/setup-android@v2

    - name: Install dependencies
      run: npm install

    - name: Cache Gradle packages
      uses: actions/cache@v3
      with:
        path: |
          ~/.gradle/caches
          ~/.gradle/wrapper
        key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
        restore-keys: |
          ${{ runner.os }}-gradle-

    - name: Make gradlew executable
      run: chmod +x android/gradlew

    - name: Generate Release APK
      run: |
        cd android
        ./gradlew assembleRelease

    - name: Upload APK
      uses: actions/upload-artifact@v3
      with:
        name: methode-j-release-apk
        path: android/app/build/outputs/apk/release/app-release.apk

    - name: Create Release
      id: create_release
      uses: actions/create-release@v1
      if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: v1.0.${{ github.run_number }}
        release_name: Méthode J v1.0.${{ github.run_number }}
        body: |
          ## 🎉 Méthode J - Application de révision espacée
          
          ### ✅ Fonctionnalités
          - **Algorithme de révision espacée** avec coefficient exponentiel
          - **Gestion des UE** avec couleurs et paramètres personnalisables
          - **Système de gamification** (points, badges, niveaux)
          - **Calendrier** avec visualisation des révisions
          - **Notifications** quotidiennes et pré-examen
          - **Statistiques** détaillées avec graphiques
          - **Export/Import** des données
          - **Interface sombre** moderne

          ### 📱 Installation
          1. Téléchargez le fichier `methode-j-release.apk`
          2. Activez "Sources inconnues" dans les paramètres Android
          3. Installez l'APK
          4. Profitez de vos révisions espacées ! 🚀

          ### 🎯 Algorithme
          - Intervalles : 1, 7, 14, 30, 90, 180, 365 jours
          - Coefficient selon la note (0.2x à 2.5x)
          - Seuil éliminatoire configurable (défaut 9/20)
        draft: false
        prerelease: false

    - name: Upload Release Asset
      uses: actions/upload-release-asset@v1
      if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ steps.create_release.outputs.upload_url }}
        asset_path: android/app/build/outputs/apk/release/app-release.apk
        asset_name: methode-j-release.apk
        asset_content_type: application/vnd.android.package-archive
